- Change both taxis to gymnasium api
- Test if we can add namespace 'our_gym_environments' before name in id without problem
- Create a custom mapping for each environment and test it on play mode
- See if we can pass info to the render method again